schema {
  query: Query
  mutation: Mutation
}

type Emissions {
  history: [AWSTimestamp!]!
}

type Mutation {
  emit(profile: String): AWSTimestamp!
}

type Query {
  emission(profile: String): Emissions!
  settings(profile: String): User!
}

type User {
  profile: String!
}
